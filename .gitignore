# deps
node_modules/
.pnpm-store/
__pycache__/
*.py[cod]
.venv/
venv/

# builds
dist/
build/
.next/
out/
*.tsbuildinfo

# env & secrets  — NEVER commit real credentials
.env
.env.*
!.env.example
*.pem
*.key
!**/public*.pem

# data & artifacts
/data/
/uploads/
# Real patient-adjacent inputs for manual integration testing (CLAUDE.md rule 8: no real
# patient data in the repo, ever) — a real prescription photo and a real voice clip.
/eval/real-inputs/
infra/seed/namaste/*.m4a
*.sqlite
*.db
minio-data/
pgdata/
services/ai/.cache/
/models/
services/*/models/
*.nemo
*.gguf

# tooling
.turbo/
coverage/
.pytest_cache/
.ruff_cache/
.mypy_cache/
playwright-report/
test-results/

# local dev runtime (make dev / make demo)
.cache/
.dev-logs/
.dev-pids/

# local agent-orchestration tool state — not part of this project
/hive/
/roster.json
/roster-backups/

# os / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
pydantic-settings==2.15.0
structlog==26.1.0
httpx==0.28.1
PyYAML==6.0.3
python-multipart==0.0.32
asyncpg==0.31.0
python-dateutil==2.9.0.post0

celery==5.6.3
redis==8.1.0
boto3==1.40.15

# C2 medical entity extraction (docs/15-ai-stack.md). Unlike paddlepaddle (OCR local tier,
# below), gliner's torch dependency does ship a Python 3.14 wheel — verified on this machine:
# installs clean, downloads Ihor/gliner-biomed-base-v1.0 (~756MB), and runs real inference.
# app/extract/gliner_ner.py still lazy-imports it so a deployment without it installed just
# can't serve this one tier (same defensive pattern as ocr/local.py).
gliner==0.2.28

pytest==9.1.1
pytest-asyncio==1.4.0

# Offline OCR tier only. Not required to import or start this service; app/ocr/local.py
# imports these lazily inside the function body, same pattern as services/ai's offline ASR.
#
# Verified live, and the "no wheel for 3.14" claim this comment used to make is WRONG —
# transformers, torch and torchvision all install and run cleanly on this machine's Python
# 3.14. What's actually broken, diagnosed two levels deep as of 2026-09-07: PaddlePaddle/
# PaddleOCR-VL's own remote modeling code never registers its config class into transformers'
# AutoModelForImageTextToText mapping, so app/ocr/local.py's `pipeline("image-text-to-text",
# ...)` call fails on every transformers version tried (4.55.0/4.57.1/5.13.1). Loading the
# model's own class directly instead (`AutoModelForCausalLM.from_pretrained(...,
# trust_remote_code=True)`, bypassing pipeline() entirely) was tried live as the suspected
# real fix, against transformers==5.13.1/torch==2.14.0 below plus einops and pillow (both
# required, neither listed here before this was tried) — the processor loads, but the model
# constructor hits a *second*, unrelated incompatibility: transformers 5.13.1 removed the
# 'default' rope_type PaddleOCR-VL's vendored code still references (`KeyError: 'default'` in
# transformers.modeling_rope_utils.ROPE_INIT_FUNCTIONS). Full diagnosis in
# docs/06-document-ai.md's "OCR reality check". Not fixed — needs either a transformers version
# satisfying both constraints (not found among the three tried) or a local patch to the
# vendor's remote code. Separately, and already fixed in local.py regardless of the above: the
# pipeline() call did not pass trust_remote_code=True, which made it hang forever (not fail) on
# an interactive y/N prompt in any non-interactive process — a container, this service under
# uvicorn, CI.
# paddlepaddle==3.3.1
# transformers==5.13.1
# torch==2.14.0
# torchvision==0.24.0
# einops
# pillow
